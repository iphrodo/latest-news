## 1. Налаштування проєкту

- [x] 1.1 Ініціалізувати проєкт на останньому стабільному Nuxt (`npx nuxi@latest init`), базова структура (`pages/` або `app.vue`, `server/api/`)
- [x] 1.2 Встановити залежність для парсингу RSS/XML (наприклад, `fast-xml-parser` або аналог)
- [x] 1.3 Налаштувати `git init` та базовий `.gitignore` (node_modules, .nuxt, .output тощо)

## 2. Nitro-ендпоінт новин (`server/api/news.ts`)

- [x] 2.1 Реалізувати модуль отримання RSS-фіду BBC Sport Football
- [x] 2.2 Реалізувати парсинг XML у структуровані об'єкти новин (заголовок, уривок, дата, посилання)
- [x] 2.3 Додати фільтрацію новин за ключовими словами АПЛ (назва ліги, клуби)
- [x] 2.4 Реалізувати `server/api/news.ts`, що повертає до 10 найновіших відфільтрованих новин у форматі JSON
- [x] 2.5 Додати обробку помилок ендпоінта (недоступність джерела, тайм-аут) з коректним HTTP-статусом (`createError`)

## 3. Клієнтська посадкова сторінка (Vue)

- [x] 3.1 Створити сторінку лендінгу (заголовок, контейнер для списку новин)
- [x] 3.2 Реалізувати запит до `/api/news` через `useFetch`/`$fetch` при завантаженні сторінки
- [x] 3.3 Реалізувати компонент картки новини (заголовок, уривок, дата, посилання, що відкривається в новій вкладці)
- [x] 3.4 Реалізувати стан завантаження (індикатор до отримання відповіді)
- [x] 3.5 Реалізувати стан помилки з кнопкою "Спробувати ще раз"
- [x] 3.6 Додати адаптивні стилі (коректне відображення в один стовпець на екранах <480px)

## 4. Перевірка

- [x] 4.1 Вручну перевірити сценарій із 10+ новинами від джерела
- [x] 4.2 Вручну перевірити сценарій із недоступним джерелом новин (симуляція помилки)
- [x] 4.3 Вручну перевірити адаптивність на мобільному розмірі екрана
- [x] 4.4 Звірити реалізацію зі сценаріями в `specs/epl-news-landing/spec.md`
