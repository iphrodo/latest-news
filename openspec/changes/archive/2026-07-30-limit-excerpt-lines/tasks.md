## 1. Обмеження уривку до 4 рядків

- [x] 1.1 Додати CSS line-clamp (обмеження на 4 рядки, обрізання з `...`) до `.news-card__excerpt` у `app/components/NewsCard.vue`, включно з крос-браузерними властивостями (`display: -webkit-box`, `-webkit-line-clamp`, `-webkit-box-orient`, `overflow: hidden`).
- [x] 1.2 Перевірити, що обмеження застосовується однаково для стрічки АПЛ і всіх табів категорій, оскільки всі вони використовують спільний компонент `NewsCard.vue`.

## 2. Перевірка

- [x] 2.1 Запустити повний набір тестів (`npx vitest run`) і переконатися, що існуючі тести не зламались.
- [x] 2.2 Вручну перевірити в браузері категорію з довгим описом (наприклад Technology або Artificial Intelligence), що картка більше не розтягується на весь текст статті.
